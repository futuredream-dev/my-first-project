# Linux 学习记录

## 第一天

今天完成了：

- Linux虚拟机联网
- SSH连接
- VS Code远程开发
- Git安装
- Git仓库初始化

学习命令：

- ls 查看目录
- cd 进入文件（扩展用法 . 查看当前目录  ..回退至上一文件  ~cd表示用户的home目录 cd ~或cd ~/某文件）
- pwd 查看当前所在的目录路径
- mkdir 创建文件夹（可以在指定用户中创建文件夹例如mkdir ./test表示在当前目录下创建文件夹  mkdir ~/test2 表示在根目录下创建文件夹test 若当前已经在一个文件夹中可以回退至上一级创建文件及 mkdir ../test）（mkdir -p linux/good/666可以在home目录里创建一连串的文件）
- touch 创建文件
注意：ctrl+l 可以直接清除当前终端的命令（快捷键）
注意：在我们用ls -l查看文件是前置位d的是文件夹 前置位-的是文件