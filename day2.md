Linux学习记录
第二天

1.cat命令 查看文件内容，一次性全部列出
2.more命令 查看文件内容，可以通过空格翻页查看，比较直观方便 按q键可以退出状态
3.cp命令 复制文件/文件夹
         -r选项，用于文件夹的复制
         参数1，linux路径，表示被复制的文件或文件夹
         参数2，linux路径，表示要被复制去的地方
4.mv命令 移动文件或文件夹（可以兼顾改名的用法）
5.rm命令 删除文件，文件夹
       -r选项 用于文件夹的删除
       -f表示force，强制删除（普通用户删除是不会显示提示的，只有root权限下才会显示删除提示）所以一般用户用不到-f选项
  rm命令支持通配符*，用来做模糊匹配
       例如 test*，表示匹配任何以test开头的内容
            *test，表示匹配任何以test结尾的内容
            *test*，表示匹配任何包含test内容的文件
6.which命令 查找命令的程序文件的存放地址
7.find命令 查找文件中带有指定内容的文件 find 起始路径 -name "被查找文件命"
    支持通配符*
     test*，表示匹配任何以test开头的内容
            *test，表示匹配任何以test结尾的内容
            *test*，表示匹配任何包含test内容的文件
    可以按照文件大小去查找文件 +、-表示大于和小于
    n表示大小数字
    k、m、g表示kb、mb、gb
        例如 查找小于10kb的文件： find / -size -10k  以此类推